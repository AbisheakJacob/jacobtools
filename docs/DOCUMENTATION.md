# DOCUMENTATION

## Build

Delete **dist** folder in the root directory.

```powershell
python -m build
```

## Local Installation

In the root directory, run the below command:

```powershell
py -m pip install .
```


