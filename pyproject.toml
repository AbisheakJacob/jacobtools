[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ["py313"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["jacobtools*"]
exclude = ["jacobtools_test*"]